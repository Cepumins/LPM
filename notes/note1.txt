function functionA() {
  timer.start('functionA');
  // Simulate work
  for (let i = 0; i < 1e6; i++);
  functionB();
  timer.stop('functionA');
}

function functionB() {
  timer.start('functionB');
  // Simulate work
  for (let i = 0; i < 5e5; i++);
  timer.stop('functionB');
}

// Call functions multiple times
functionA();

// Report individual and total times for all functions
timer.reportAll();